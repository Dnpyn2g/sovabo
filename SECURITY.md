# Security Policy

## 🔒 Reporting Security Vulnerabilities

Если вы обнаружили уязвимость безопасности, **НЕ создавайте публичный Issue**.

Вместо этого:
1. Отправьте email на: [your-email@example.com]
2. Или свяжитесь в Telegram: [@your_username]

Включите в сообщение:
- Описание уязвимости
- Шаги для воспроизведения
- Потенциальное влияние
- Предложения по исправлению (если есть)

## ✅ Supported Versions

| Version | Supported          |
| ------- | ------------------ |
| Latest  | ✅ Active support |
| < 1.0   | ❌ Not supported  |

## 🛡️ Security Best Practices

При деплое бота:

### 1. Environment Variables
```bash
# ❌ НЕ ДЕЛАЙТЕ ТАК
BOT_TOKEN="123456:ABC..."  # в коде

# ✅ ДЕЛАЙТЕ ТАК
BOT_TOKEN=os.getenv("BOT_TOKEN")  # из .env
```

### 2. Database Security
- Используйте `DB_TIMEOUT` для предотвращения блокировок
- Регулярно делайте бэкапы
- Не храните пароли в открытом виде

### 3. API Tokens
- Никогда не коммитьте `.env` файл
- Используйте разные токены для dev/prod
- Регулярно ротируйте токены

### 4. Server Credentials
- Файлы `99.txt`, `servera.txt` должны быть в `.gitignore`
- Используйте SSH ключи вместо паролей где возможно
- Ограничивайте права доступа (`chmod 600`)

### 5. User Input
- Всегда валидируйте пользовательский ввод
- Используйте параметризованные SQL запросы
- Экранируйте HTML в сообщениях

### 6. Rate Limiting
- Бот уже включает защиту от спама
- Настройте firewall на сервере
- Используйте Cloudflare для web интерфейса

## 🚨 Known Security Considerations

1. **SQLite Limitations**: Для высоконагруженных систем рекомендуется PostgreSQL
2. **SSH Connections**: Провизионинг использует Paramiko - регулярно обновляйте
3. **Payment Processing**: Валидация Telegram Stars хэшей критична

## 📋 Security Checklist

Перед деплоем:
- [ ] `.env` файл настроен и защищён
- [ ] `bot.db` не в публичном доступе
- [ ] Все секреты в `.gitignore`
- [ ] HTTPS для web интерфейса
- [ ] Firewall настроен
- [ ] Регулярные бэкапы настроены
- [ ] Мониторинг логов включён

## 🔄 Update Policy

Критичные уязвимости патчатся в течение 48 часов.
Обновления безопасности анонсируются в [Releases](https://github.com/yourusername/sova-vpn-bot/releases).
