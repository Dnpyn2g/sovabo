# Руководство по оплате через Telegram Stars

## Что добавлено

В бота добавлена поддержка оплаты через **Telegram Stars** - встроенную валюту Telegram.

## Обновления

### 1. Зависимости
Обновлена версия `python-telegram-bot` до `21.9` в `requirements.txt` для поддержки Telegram Stars.

**Установка:**
```bash
pip install -U python-telegram-bot[rate-limiter,job-queue]
```

### 2. Новые обработчики

#### `handle_pre_checkout_query()`
- Обрабатывает предоплатную проверку перед завершением платежа
- Всегда возвращает `ok=True` (можно добавить дополнительную валидацию)

#### `handle_successful_payment()`
- Обрабатывает успешный платёж через Stars
- Зачисляет средства на баланс пользователя
- Отправляет уведомление рефереру о бонусе
- Обновляет статус депозита в БД

#### `cmd_paysupport()`
- Команда `/paysupport` с информацией о возврате средств
- Требуется по условиям Telegram для всех ботов с платежами

### 3. Меню пополнения

В меню "💰 Пополнение" добавлена кнопка **⭐️ Telegram Stars**.

#### Доступные пакеты:
- 100 ⭐️ = $2
- 250 ⭐️ = $5
- 500 ⭐️ = $10
- 750 ⭐️ = $15
- 1250 ⭐️ = $25
- 2500 ⭐️ = $50

### 4. Курс конвертации

Используется курс: **1 Star ≈ $0.02**

Можно изменить в коде (строка 2456):
```python
star_rates = [
    (100, 2.0),    # 100 stars = 2 USD
    (250, 5.0),    # 250 stars = 5 USD
    # ...
]
```

## Как работает

1. **Пользователь выбирает Stars** → Меню → Пополнение → ⭐️ Telegram Stars
2. **Выбирает пакет** → Например, 500 ⭐️ = $10
3. **Бот создаёт счёт** → Через `send_invoice()`
4. **Пользователь оплачивает** → В окне Telegram
5. **Pre-checkout** → Бот подтверждает транзакцию
6. **Успешный платёж** → Баланс зачисляется автоматически

## База данных

Депозиты Stars хранятся в таблице `deposits`:
```sql
deposit_type = 'stars'
invoice_id = 'stars_{user_id}_{timestamp}'
```

## Команды

- `/start` - Главное меню
- `/paysupport` - Информация о платежах и возврате средств
- `/web` - Веб-интерфейс

## Тестирование

1. Запустите бота: `python main.py`
2. Откройте бота в Telegram
3. Меню → 💰 Пополнение → ⭐️ Telegram Stars
4. Выберите пакет и оплатите

**Примечание:** В тестовом режиме Telegram может не списывать реальные Stars.

## Возможные доработки

1. **Кастомная сумма Stars** - добавить возможность ввести произвольное количество звёзд
2. **Динамический курс** - получать курс Stars/USD из API
3. **История платежей** - добавить просмотр истории платежей Stars
4. **Возврат средств** - добавить функцию возврата через `refund_star_payment()`

## Поддержка

Если у пользователей возникнут вопросы по платежам, они могут:
- Использовать команду `/paysupport`
- Написать в поддержку через `/support`
- Связаться с администратором

## Соответствие требованиям Telegram

✅ Команда `/paysupport` добавлена  
✅ Pre-checkout обработчик реализован  
✅ Successful payment обработчик реализован  
✅ Информация о возврате средств предоставлена  

## Безопасность

- Все депозиты логируются в БД
- Транзакции обрабатываются атомарно
- Дублирование платежей предотвращено проверкой статуса
- Payload содержит ID депозита для отслеживания
